language: generic
install:
- "gem install html-proofer"
- "sudo apt-add-repository ppa:duggan/jo --yes && sudo apt-get update -q --yes && sudo apt-get install jo --yes"
script:
- "scripts/cibuild"
env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true
addons:
  apt:
    packages:
    - jq
sudo: true

